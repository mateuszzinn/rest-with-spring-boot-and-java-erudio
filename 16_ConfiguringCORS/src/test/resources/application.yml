server:
  port: 8888
cors:
  originPatterns: http://localhost:8080,http://localhost:3000,https://mateus.com.br
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: none
    # properties:
      # hibernate:
        # dialect: org.hibernate.dialect.MySQLDialect
      show_sql: false
springdoc:
  pathsToMath: /api/**/v1/**
  swagger-ui:
    use-root-path: true
    path: /s
